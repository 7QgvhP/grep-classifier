# 変更履歴 (Change Log)

本プロジェクト（C-Grep Classifier）のすべての変更は、このファイルに記録されます。

## [2.5.0] - 2026-07-30
### 追加
* サイドバーの検索結果に、全カテゴリの**合計件数**を表示するようにしました。
  * カテゴリ一覧の上部に `合計 21 件 / 2 ファイル` のように、合計の一致件数と対象ファイル数を表示します。
  * 関数名一覧モードでは `合計 21 件 / 3 関数` のように、合計件数と重複を除いた関数の数を表示します。
  * エディタの検索結果一覧では、先頭行に同等の情報（`21 件の一致 / 2 ファイル`）を従来から表示しています。

## [2.4.3] - 2026-07-30
### 修正
* v2.4.1 で修正した「前置マクロ付きの変数宣言が『その他』に分類される」不具合が、**インクルードガードで囲まれたヘッダファイルでは解消されていなかった**問題を修正。
  * v2.4.1 の救済条件が「式文の親が `translation_unit` の直下であること」だったため、`#ifndef` / `#define` / `#endif` で囲まれたヘッダでは `expression_statement > preproc_ifdef > translation_unit` となり、条件を満たしていませんでした。
  * 判定を「祖先に関数本体（`compound_statement`）が現れないこと」に一般化し、インクルードガードや `#ifdef` で囲まれている場合も救済されるようにしました。
  * 実際のヘッダ構造（共用体のビットフィールド定義＋多数の `GLOBAL` 宣言）を用いた検証で、インクルードガードあり・なし・`#ifdef` 内・`typedef` 併用など6パターンすべてが「定義」に分類されることを確認しています。

## [2.4.2] - 2026-07-29
### 修正
* エディタに表示した検索結果一覧で、`{` などの括弧が赤字で表示されることがある問題を修正。
  * 原因は VS Code の**ブラケットペアの色分け**機能です。結果一覧にはコードの一部（`void func(void) {` など）がそのまま含まれるため、対応する閉じ括弧が存在しない括弧が「不一致」と判定され赤字で強調されていました。検索結果としての意味はありません。
  * 検索結果ドキュメント専用の言語ID `cgrep-result` を登録し、この言語に限り括弧の色分け・ガイド・対応括弧の強調を無効化しました。他のファイルの表示には影響しません。

## [2.4.1] - 2026-07-29
### 修正
* `GLOBAL BYTE hoge;` のような前置マクロ付きの変数宣言が、「定義」ではなく「その他」に分類されることがある不具合を修正。
  * 原因は、tree-sitter がこれらの宣言を `declaration` ではなく **`expression_statement`（式文）として解釈する**場合があり、分類ロジックに式文に対する判定規則が無かったことです。v1.8.1 で追加した救済処理は `ERROR` ノード配下のみを対象としていたため、このケースが漏れていました。
  * 発生条件は、`union` / `struct` の本体付き定義（`GLOBAL union hogestruct { … } hoge2;`）や `typedef union { … } HOGE_T;` がファイル内にある場合です。これらの直後の `GLOBAL <型> <変数>;` が式文として解釈されていました。
  * C言語ではファイルスコープに式文は存在し得ないため、`translation_unit` 直下の `expression_statement` に現れた識別子は宣言の解釈失敗とみなし、「定義」として救済するようにしました。
  * 関数内の式文は対象外のため、`hoge.a = 1;` などの既存の分類には影響しません。

## [2.4.0] - 2026-07-29
### 追加
* キーワードが出現する関数名のみを一覧表示する「関数名一覧」モードを追加。
  * 一致箇所を行単位で列挙する代わりに、キーワードが出現する関数名を重複なくカテゴリ別に表示します。「どの関数がこの変数を書き換えているか」を俯瞰できます。
  * サイドバー検索窓の「関数名一覧を表示」トグルボタン、またはコマンドパレットの `C-Grep: 関数名一覧の表示を切り替え` で切り替えます。切り替えはサイドバーとエディタの結果一覧の双方に反映されます。
  * 関数名を選択（サイドバー）、または `Enter` / `Ctrl + クリック`（エディタ）すると、その関数内の最初の一致箇所へジャンプします。
  * 切り替え時に再検索は行わず、保持している検索結果を描画し直します。
### 変更
* エディタの検索結果一覧が、設定 `cGrepClassifier.showEnclosingFunction` に従うように変更。
  * 従来はこの設定にかかわらず、エディタ側には常に関数名が表示されていました。設定を `false` にすると関数名の列自体が出力されなくなります。

## [2.3.0] - 2026-07-29
### 追加
* 検索結果をエディタのタブにテキストとして一覧表示する機能を追加（VS Code の「検索エディター」に相当）。
  * サイドバー検索窓の右に追加した「検索結果をエディタで開く」ボタン、またはコマンドパレットの `C-Grep: 検索結果をエディタで開く` から表示します。
  * カテゴリ → ファイル → 一致箇所の順に一覧表示し、行番号・所属関数・コードを桁揃えして出力します。
  * 結果行にカーソルを置いて `Enter` を押すか、`Ctrl + クリック`（定義へ移動）で該当箇所へジャンプします。ジャンプ時のハイライトはサイドバー経由の場合と同じです。
  * 読み取り専用のドキュメントで、再検索すると開いているタブの内容が自動的に更新されます。
  * テキストであるため、結果の選択・コピーやファイルへの保存が可能です。
  * サイドバーでの検索結果表示は従来通り利用できます。

## [2.2.2] - 2026-07-28
### 修正
* サイドバーの幅を変更すると、検索結果の行番号とコードの間隔（コードの開始位置）が微妙にずれる不具合を修正。
  * 原因は、コード列の `flex-basis` が `auto`（＝コード全文の幅）だったことです。行の内容が常に幅を超過している状態となるため Flexbox の縮小処理が働き、**行番号列や関数名列まで縮小対象**になっていました。縮小量は不足量に比例して決まるため、サイドバー幅を変えるたびに各列の幅が変動していました。
  * コード列を `flex: 1 1 0`（基準幅0）とし、行番号列を `flex: 0 0 auto`（縮小しない）に変更。伸縮するのはコード列のみとなり、行番号と関数名の位置が幅によらず固定されます。
  * 関数名列の上限幅も、幅に連動する `45%` から固定値 `120px` に変更しました。
  * あわせて、サイドバーを極端に狭めた際にコードが完全に消えないよう、コード列に最小幅を設定しました。

## [2.2.1] - 2026-07-28
### 修正
* コメント・文字列リテラル・マクロ定義の置き換え値が含まれる行で、同一箇所が重複して検出され、さらに誤ったカテゴリで分類される不具合を修正。
  * 原因は、これら3種類のノードの検索処理が、ノード自身の範囲ではなく**行全体**を走査していたことです。
  * 例1: `printf("hoge=%d", hoge);` が2箇所ではなく3箇所として検出されていました。
  * 例2: `printf("hoge"); printf("hoge");` のように該当ノードが1行に2つある場合、行全体の走査が2回行われ4箇所として検出されていました。
  * 例3: `hoge = 1; // hoge を更新` では、コード部分の `hoge` が「出力」に加えて「コメント」としても重複登録されていました。
  * ノードが占める列範囲内のみを走査するように修正し、複数行コメントについても行ごとに正しい範囲（開始行は開始列以降、終了行は終了列まで、中間行は行全体）を算出するようにしました。
  * なお `hoge = hoge + 3;` のような識別子どうしの複数出現は、従来から正常に検出されており影響ありません。

## [2.2.0] - 2026-07-28
### 追加
* 検索結果の各項目に、一致箇所が属する**関数名**を表示する機能を追加。
  * 行番号とコードスニペットの間に、その一致箇所を含む関数の名前を表示します（例: `42  update_status()  status = 1;`）。「この変数がどの関数で書き込まれ、どの関数で読み出されているか」を一覧上で把握できます。
  * 関数外のグローバル変数宣言・マクロ定義・ファイル先頭のコメントなど、関数に属さない箇所では表示されません。
  * `void *func(void)` のようにポインタ宣言子が入れ子になる関数定義にも対応しています。
  * 表示のON/OFFは設定 `cGrepClassifier.showEnclosingFunction`（既定値 `true`）で切り替えられます。

## [2.1.0] - 2026-07-28
### 追加
* 検索対象を設定でカスタマイズできる機能を追加。VS Code の設定画面（`C-Grep Classifier`）から変更できます。
  * `cGrepClassifier.includePattern`: 検索対象とするファイルのglobパターン（既定値 `**/*.{c,h}`）。`.inc` など独自拡張子を扱うプロジェクトに対応。
  * `cGrepClassifier.excludePatterns`: 検索対象から除外するglobパターンの一覧（既定値 `["**/node_modules/**"]`）。ビルド生成物や外部ライブラリを除外可能。
  * `cGrepClassifier.useDefaultExcludes`: VS Code標準の除外設定（`files.exclude` / `search.exclude`）も併せて適用するか（既定値 `true`）。
### 修正
* エディタで編集中（未保存）の内容が検索結果に反映されず、保存前のディスク上の内容で分類・ジャンプしてしまう問題を修正。
  * 検索対象ファイルがエディタで開かれており未保存の変更がある場合、ディスクではなくエディタ上のテキストを解析するように変更。編集途中でも正しい行番号・分類結果が得られます。

## [2.0.0] - 2026-07-27
### 変更
* 保守性の向上を目的とした大規模なリファクタリングを実施。**機能および分類ロジックの挙動に変更はありません。**
  * **Webview資産の外部ファイル化**: `extension.ts` 内に約610行のテンプレート文字列として埋め込まれていた HTML / CSS / JavaScript を、`media/webview.html`・`media/main.css`・`media/main.js` へ分離。プレースホルダ置換方式（`{{nonce}}` 等）で読み込むように変更し、`extension.ts` を約1,200行から約270行に削減。
  * **TypeScriptのモジュール分割**: 分類ロジックを `src/classifier.ts`、検索・抽出ロジックを `src/matcher.ts`、型とカテゴリ定義を `src/types.ts` へ分離。
  * **分類ロジックの構造化**: `classifyIdentifier` 内に羅列されていた判定を `checkOutput` / `checkInput` / `checkDefinition` の3関数へ分割し、「出力 → 入力 → 定義」の優先順位をコード構造として明示。
  * **重複コードの集約**: コメント・マクロ定義値・文字列リテラルの3箇所に重複していた「同一行内の複数出現位置の検出」処理を `collectOccurrences` 関数へ統合。正規表現オブジェクトの都度生成も解消。
  * **カテゴリ定義の一元化**: 拡張機能側とWebview側で二重管理されていた分類カテゴリ（名称・表示順・配色クラス）を `CATEGORIES` 定数に統合し、Webviewへは HTML 生成時に注入する方式へ変更。
  * **ファイル読込の非同期化**: `fs.readFileSync` による同期読み込みを `vscode.workspace.fs.readFile` へ置き換え、検索中の拡張ホストのブロッキングを解消。あわせて `TextDecoder` を文字コード単位でキャッシュするように改善。
  * **Webviewの状態保存方式の変更**: 描画済みHTML全体を保存していた方式を、一致情報の配列・アコーディオンの折りたたみ状態・選択位置のみを保存し復元時に再描画する方式へ変更。
  * **選択処理の一本化**: キーボード操作とマウスクリックで二重に実装されていた項目選択処理を `selectMatchItem` に統合。
  * Content Security Policy からスタイルの `'unsafe-inline'` 指定を削除（インラインスタイルの廃止に伴う）。
  * `package-lock.json` のバージョンが `0.0.1` のまま実体と乖離していた問題を是正。

## [1.12.3] - 2026-07-25
### 修正
* サイドバーのリサイズ時に検索結果のテキストが微妙に位置ズレを起こす不具合を修正。
  * CSS の `font-size` に相対単位（`em`）を使用していたことが原因で、リサイズ時にサブピクセル丸め値が変動し文字位置が揺れていた。すべて固定ピクセル値（`px`）に変更することで解消。

## [1.12.2] - 2026-06-26
### 修正
* 検索結果の初期展開および複数マッチ処理のバグを修正。
  * 検索結果の表示時、アコーディオン（カテゴリコンテナ）を初期状態で自動展開（オープン）するように変更。これにより、検索実行後にマウスを使わずすぐ `ArrowDown` キーで項目巡回を開始できるように改善。
  * 同一行内に同じキーワードが複数出現する場合（コメントや文字列リテラル内）、それぞれの出現位置を正確に個別に検出してハイライト・ジャンプができるよう修正。

## [1.12.1] - 2026-06-25
### 修正
* 検索結果のキーボード巡回操作において、入力欄とのフォーカス遷移およびクリック時のフォーカス制御不具合を修正。
  * 検索入力欄で `ArrowDown` キーを押した際、自動的に結果リストの1番目の項目にフォーカスが移行してプレビューされるよう改善。
  * リスト最上部の項目で `ArrowUp` キーを押した際、検索入力欄に自動でフォーカスが戻るよう挙動を修正。
  * マウスクリックでの項目選択時、エディタにフォーカスが奪われないよう `preserveFocus: true` を適用し、そのままキーボードでの上下移動が継続できる仕様に修正。

## [1.12.0] - 2026-06-25
### 追加
* 検索結果画面において、キーボードの上下キー（`ArrowUp` / `ArrowDown`）で一致項目を巡回選択し、エディタ上に自動でプレビュー表示する機能を追加。
  * 移動と同時にエディタで該当行を表示（サイドバーにフォーカスを維持）し、`Enter` キーを押すことでエディタにフォーカスを移す挙動をサポート。
  * マウスクリックでの選択時にも巡回位置が同期され、アコーディオンの開閉状態（表示されている要素のみ）を自動判別して巡回可能。

## [1.11.0] - 2026-06-25
### 変更
* 検索入力ボックスおよび「単語全体に一致 (Match Whole Word)」トグルボタンのデザイン・構造をVS Code標準UIに準拠する形に最適化。
  * 入力枠内にトグルボタンを美しく内包する `search-input-control` 構造に変更し、フォーカス時に全体が青く囲まれるように改善。
  * トグルボタンのアイコンを、VS Code公式アイコンフォント（Codicons）の `whole-word` アイコンと完全に同一のSVGに変更。

## [1.10.1] - 2026-06-25
### 変更
* 選択テキスト検索のショートカットキーを `Ctrl+Alt+F` (Mac: `Cmd+Alt+F`) から `Alt+Q` (Mac: `Alt+Q`) に変更。
  * VS Code標準の他のショートカットキーとの衝突を避け、片手でより押しやすい配置に変更。

## [1.10.0] - 2026-06-25
### 追加
* C/Hファイルのエディタ上で単語（テキスト）を選択した状態でショートカットキーを押すと、その単語を検索クエリとして自動でデータフロー分類検索を実行する機能を追加。
  * 既存のショートカットキー `Ctrl+Alt+F` (Mac: `Cmd+Alt+F`) を拡張し、テキスト選択時には自動検索・結果表示を実行し、テキスト未選択時にはサイドバーにフォーカスを当てる動作をサポート。

## [1.9.2] - 2026-06-18
### 修正
* `#define HOGE (hoge)` などのように、カッコ等の記号で囲まれたマクロ定義の置き換え値（右辺）が、完全一致（単語全体に一致）検索でヒットしないバグを修正。
  * `preproc_arg` ノード（マクロ定義の右辺値）に対する完全一致判定の際、単純な文字列比較ではなく正規表現の単語境界（`\b`）を用いるように修正。

## [1.9.1] - 2026-06-17
### 修正
* 複数行コメント（`/** ... */`）の中に検索キーワードが含まれている場合、実際の出現行ではなくコメントの開始行が検索結果に表示されてしまうバグを修正。
  * コメントノード内のテキストを行単位に分割して個別に探索し、正しい出現行の行番号および正確な文字位置を抽出して表示・ハイライトできるように対応。

## [1.9.0] - 2026-06-17
### 追加
* 検索結果の分類対象として、VS Code標準の検索機能のように「完全一致（単語全体に一致）」のみを抽出する機能を追加。
  * 検索窓の右側に「単語全体に一致」の切り替え用トグルボタン（`[ab]` アイコン）を追加。
  * トグルボタンの ON/OFF 状態は、VS Codeテーマに連動するスタイルを適用し、サイドバー切り替えや再起動時にも状態が保持されるように対応。
  * コメントや文字列リテラル内の検索時には、正規表現の単語境界判定を用いて適切に単語全体の一致を評価。

## [1.8.2] - 2026-06-17
### 修正
* `#define HOGE hoge` のように、プリプロセッサマクロ定義の置き換え値（右辺）に検索キーワードが含まれている場合、検索結果にヒットしない（カウントされない）バグを修正。
  * `preproc_arg` ノード（マクロ定義の右辺値）も検索対象の識別子として抽出するように検索条件を改善。

## [1.8.1] - 2026-06-17
### 修正
* C言語の静的解析において、一部の定義ノードが「その他」に誤判定される不具合を修正。
  * `typedef`（型定義）、`enum`定数メンバー、マクロ引数が「定義」として正しく分類されるように条件を追加。
  * 複数変数宣言の2番目以降の変数名、および前置マクロ（例: `GLOBAL SBYTE e;`）適用時の構文エラー発生箇所における変数名について、正常に「定義」として救済・分類されるようロジックを強化。

## [1.8.0] - 2026-06-14
### 変更
* プロジェクトのクリーンアップおよびリファクタリング。
  * 開発用テンプレートの不要なドキュメント（`vsc-extension-quickstart.md`）および過去の不要な `.vsix` パッケージ群を削除。
  * `classifyIdentifier` 関数の引数を `node` のみに削減し、内部判定を `node.text` へ直接参照するようにリファクタリング。

## [1.7.1] - 2026-06-13
### 修正
* 同一行内に検索キーワード（例: `hoge`）と、それを部分文字列として含む別の変数（例: `hogemax`）が混在している場合に、双方が「出力」カテゴリ等に重複誤判定されてしまうバグを修正。
  * `classifyIdentifier` 内の所属判定ロジックを、文字列の部分一致（`includes`）から、ASTの物理的位置の包含関係（子孫ノード判定）へ切り替えることで、同一行内での分類の正確性を向上。

## [1.7.0] - 2026-06-13
### 追加
* 構造体の要素名（`field_identifier`）単体での検索・データフロー分類（入力・出力・定義など）に完全対応。
  * `youso` などの記号を含まない単語のみで検索した場合にも、`kouzoutai.youso` のような構造体メンバーアクセス箇所を正常に検出して分類するように検索ロジックを改善。
  * 構造体定義箇所でのメンバー定義に対しても、正しく「定義」カテゴリとして抽出・表示するように対応。

## [1.6.0] - 2026-06-13
### 変更
* 検索結果の第二階層である「ファイル名トグル」の表示順を、出現順ではなく「ファイル名の辞書順（アルファベット順）」でソートして表示するように変更。

## [1.5.1] - 2026-06-10
### 修正
* 構造体メンバーアクセス（`hoge.A` や `hoge->A`）や配列アクセス（`hoge[0]`）で検索した際、構文解析上の制限で一致箇所がヒットしなかった問題を修正。
  * 検索キーワードに `.`, `->`, `[` などの演算子が含まれる場合、個々の識別子だけでなく、アクセス式全体のノードを検索評価対象に含めるようロジックを改善。
  * 式全体がヒットした際、内部の個別単語が二重に検索結果にリストアップされるのを防ぐ重複制御を追加。

## [1.5.0] - 2026-06-10
### 変更
* 検索結果の表示UIを VS Code 標準の検索機能に近いコンパクトなレイアウトへ刷新。
  * 検索結果をカテゴリ（「入力」「出力」など）の下で、さらに**ファイルごと**にアコーディオン（トグル）でグループ化して表示するように変更。
  * 検索結果1件あたりの表示を行番号と1行のコードスニペットのみに折りたたみ、画面をコンパクトに整理。長文のコードスニペットは自動的に三点リーダーで省略表示するよう調整。

## [1.4.0] - 2026-06-09
### 追加
* ファイル読込時の文字コード自動追従機能を追加。VS Code 側の文字コード設定（`files.encoding`）を自動参照し、Shift_JIS や EUC-JP などの UTF-8 以外のファイルにおける文字化けや解析失敗を防ぐように対応。
* サイドバーの分類カテゴリ（「入力」「出力」「定義」など）のアコーディオンを、初期状態で折りたたんだ（閉じた）状態にする機能を追加。また、アコーディオンの開閉に伴い矢印アイコン（▶ と ▼）が動的にトグルするように対応。
* サイドバーの「検索」ボタンを削除し、検索キーワード入力後に Enter キー押下のみで検索を実行するシンプルな UI に変更。

## [1.3.0] - 2026-06-07
### 追加
* 検索結果項目をクリックした際、エディタ上の該当箇所を VS Code 標準のテーマ色に適合する色でハイライト表示（デコレーション適用）する機能を追加。
### 変更
* 従来のショートカット/右クリックによる検索およびツリービュー表示を廃止し、サイドバー上部に常駐する「検索窓」から直接単語を入力してデータフロー分類を実行できる Webview View 方式へ移行。
### 修正
* Webview 内のクリックイベント（ファイル遷移およびアコーディオン開閉）がセキュリティポリシー（CSP）によってブロックされていた不具合を、イベントデリゲーションの導入によって解消。

## [1.2.0] - 2026-06-07
### 変更
* VS Code の標準検索アイコンと類似していたため、サイドバーの虫眼鏡アイコンを排除し、「分類リスト（箇条書き）」のみを表現したシンプルなデザインに変更。

## [1.1.1] - 2026-06-06
### 修正
* 変数定義初期値内の右辺値の誤判定を修正。`int temp = hoge_array[0]` のような式において、右辺値（値の参照）が「定義」として誤判定されていた件を、「入力」として優先的に判定するようロジックを改善。

## [1.1.0] - 2026-06-06
### 追加
* ショートカットキー `Ctrl + Alt + F`（Mac: `Cmd + Alt + F`）を押すことで、データフロー分類の検索ダイアログを起動する機能を追加。

## [1.0.1] - 2026-06-06
### 修正
* パッケージ化（VSIX）時の依存関係や設定ファイルの構成を整理。

## [1.0.0] - 2026-06-06
### 追加
* 初期リリース（C言語 Grep 検索結果データフロー分類拡張機能）。
* `web-tree-sitter` を用いた構文解析および「入力」「出力」「定義」「コメント」「その他」への分類ロジックの実装。
* Windows 環境下での WASM（C言語パーサー）ロード問題を解決するため、`fs.readFileSync` によるバイナリ直接ロード方式を採用。
* WASM の ABI バージョン不一致エラーを回避するため、ABI 14 互換のビルド済みパーサー WASM へ置き換え。
