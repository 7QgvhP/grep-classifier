# C-Grep Classifier (Tree-sitter対応版) クイックスタートガイド

このプロジェクトは、VS CodeでC言語の検索結果（データフロー：入力・出力・定義・コメント・その他）を正確に分類して表示する拡張機能です。
構文解析エンジンとして `web-tree-sitter` を使用しています。

## 開発の進め方

1. **プロジェクトを VS Code で開く**
   VS Code で `C:\Users\cxti2\Documents\My_App\c-grep-classifier` を開きます。（このフォルダをアクティブワークスペースに設定してください）

2. **依存関係のインストール（実行済み）**
   フォルダ内ですでに `npm install` が実行され、`web-tree-sitter` がインストールされています。また、`bin/` フォルダに必要な以下の WASM バイナリがダウンロード・コピーされて配置済みです。
   * `bin/tree-sitter.wasm`
   * `bin/tree-sitter-c.wasm`

3. **デバッグ実行**
   - キーボードの `F5` キーを押すか、デバッグサイドバーから「Run Extension」を実行します。
   - 新しいVS Codeのウィンドウ（拡張機能開発ホスト）が起動します。

4. **動作確認**
   - 起動した開発ホストウィンドウで、`C:\Users\cxti2\Documents\My_App\c-grep-classifier\test_data` フォルダ（もしくは検証したいC言語プロジェクト）を開きます。
   - `Ctrl + Shift + P` (Macは `Cmd + Shift + P`) を押し、コマンドパレットから `C-Grep: データフロー分類検索を実行` を選択します。
   - キーワードとして `hoge` を入力して実行します。
   - アクティビティバー（左端のメニュー）に追加された `C-Grep Classifier` のアイコン（虫眼鏡マーク）をクリックすると、サイドバーに検索結果がデータフロー（入力・出力・定義・コメント）ごとに分類されて表示されます。
   - ツリー内の要素をクリックすると、該当ファイルが開き、キーワードが含まれる正確な範囲が選択状態（ハイライト）になります。
